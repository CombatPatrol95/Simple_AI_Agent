version: "3.9"

services:
  devops-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount agent source for live development
      - ./devops_agent:/app/devops_agent
    restart: unless-stopped
