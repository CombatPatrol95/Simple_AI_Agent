[project]
name = "devops-agent"
version = "0.1.0"
description = "Multi-Agent DevOps System"
requires-python = ">=3.10"

[tool.pyright]
root = "."
pythonVersion = "3.10"
pythonPlatform = "Windows"
venvPath = "."
venv = "venv"
extraPaths = ["."]

[tool.pyre]
search_path = ["."]
